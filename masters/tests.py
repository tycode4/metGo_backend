from django.test import TestCase
    def setUp(self):
        ds.object.cret(
        )




    def tearDown(self):
        adsfa.obects.all().delete()



    def test(self):
        client = Client()

        reseponse = client.get('  ')
        print(response.json())

        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.json(),
            #  {'message':'SUCCESS',

        'results':[{
            'gender' : 'asdfasdf',
            ''







        }]

